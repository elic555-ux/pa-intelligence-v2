name: "Legacy Probate — use Central Scanner"
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - name: Scanner moved to the central workflow
        run: |
          echo "כל התזמונים מנוהלים כעת מהמסך דרך Central Scanner." >> "$GITHUB_STEP_SUMMARY"
          echo "לסריקה ידנית הפעל את cron-daily-mls.yml או את הכפתור באתר." >> "$GITHUB_STEP_SUMMARY"
